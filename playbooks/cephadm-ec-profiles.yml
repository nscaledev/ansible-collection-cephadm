---
- name: Define Ceph EC profiles
  hosts: mons
  gather_facts: false
  become: true
  tags:
    - cephadm
    - cephadm-ec-profiles
  tasks:
    - name: Apply EC profiles role
      ansible.builtin.import_role:
        name: stackhpc.cephadm.ec_profiles
